[project]
name = "wervelnieuws-worker"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = [
  "sqlalchemy==2.0.37",
  "pydantic-settings==2.7.1",
  "httpx==0.28.1",
  "tenacity==9.0.0",
]

[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"
